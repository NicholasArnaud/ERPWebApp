@model ERPWebApp.Models.QCDiagnosis

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>QCDiagnosis</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.QCDiagnosisName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.QCDiagnosisName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IsActive)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsActive)
        </dd>
    </dl>
</div>
<div>
    @if (this.User.IsInRole(RoleList.Administrator) || this.User.IsInRole(RoleList.FinancialManager) || this.User.IsInRole(RoleList.QCManager))
                    {
    <a asp-action="Edit" asp-route-id="@Model.QCDiagnosisId">Edit</a>
                    }
                    |
    <a asp-action="Index">Back to List</a>
                    }
</div>
